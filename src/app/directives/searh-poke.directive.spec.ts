import { SearhPokeDirective } from './searh-poke.directive';

describe('SearhPokeDirective', () => {
  it('should create an instance', () => {
    const directive = new SearhPokeDirective();
    expect(directive).toBeTruthy();
  });
});
